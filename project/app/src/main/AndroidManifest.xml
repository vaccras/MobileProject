<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">
        <activity android:name=".choixCultureActivity"></activity>
        <activity android:name=".Geo_Activity" />
        <activity android:name=".HistResultActivity" />
        <activity android:name=".HistActivity" />
        <activity android:name=".BorneActivity" />
        <activity android:name=".AideActivity" />
        <activity android:name=".ComparaisonActivity" />
        <activity android:name=".CalculsActivity" />
        <activity android:name=".choixMathActivity" />
        <activity android:name=".resultatActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".typeActivity" />
        <activity android:name=".compteActivity" />
        <activity android:name=".AddTaskActivity" />
        <activity android:name=".TableMultipActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>